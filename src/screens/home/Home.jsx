import React from 'react';
import { ActivityIndicator, StyleSheet, View, Image, Text } from 'react-native';
import { createStackNavigator } from '@react-navigation/stack';
import HomeStackScreen from '../../components/HomeStackScreen';
import theme from '../../constants/theme';

export default function HomeScreen() {
  return (
    {/* TODO: Implement separate screen file? */}
  );
}

const styles = StyleSheet.create({
  {/* TODO: See above. Implement styles in an organized way */}
});
