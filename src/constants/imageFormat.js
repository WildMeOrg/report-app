/**
 * imageFormat.js -- file formatting settings for images to be uploaded in a
 *                   form
 */
export default {
  format: 'ImageManipulator.SaveFormat.JPEG',
  maxImageWidth: 1000,
  compressionRatio: 0.8,
};
